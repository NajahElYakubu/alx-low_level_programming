#include <string.h>
#include "main.h"

/**
 * _strstr - main body
 * @haystack: string
 * @needle: substring
 * Return: result
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
